import type { FC } from "react";

const Footer: FC = () => {
    return (
        <footer className="text-center text-sm text-gray-500 py-4 border-t border-yellow-400 bg-white">
            Peña Los Mosquitos - Susinos del Páramo
        </footer>
    );
};

export default Footer;
